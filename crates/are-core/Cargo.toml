[package]
name = "are-core"
description = "ARE domain models, identifiers, and error types — no networking, no filesystem, no process execution"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde.workspace = true
thiserror.workspace = true
async-trait.workspace = true

[features]
# Future gate: enables request types for WriteFile, Execute, ProcessStatus,
# TerminateProcess. These are not part of the stable public API.
future = []

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
